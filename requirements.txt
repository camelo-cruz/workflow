# MacOS dependencies (CPU only, torch 1.10.x to satisfy pyannote.audio<1.0)
torch; sys_platform == "darwin"
torchvision; sys_platform == "darwin"
torchaudio; sys_platform == "darwin"
pytorch-lightning; sys_platform == "darwin"
pyannote.audio; sys_platform == "darwin"

# Windows dependencies (CUDA 10.2 build under CUDA 12.4 index)
--extra-index-url https://download.pytorch.org/whl/cu124
torch==1.10.0+cu102;      sys_platform == "win32"
torchvision==0.11.1+cu102; sys_platform == "win32"
torchaudio==0.10.0+cu102;  sys_platform == "win32"
pytorch-lightning==1.5.4; sys_platform == "win32"
pyannote.audio==0.0.1 ; sys_platform == "win32"

Pillow
python-dotenv
deepl
deep-translator
openai_whisper
pandas
spacy
tqdm
transformers
transliterate
openpyxl
sentencepiece
sacremoses
msal
requests
pykakasi
